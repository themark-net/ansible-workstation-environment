all:
  children:
    workstations:
      hosts:
        rocky-example:
          ansible_host: 192.168.1.10
          ansible_user: user
          ansible_become: yes
        ubuntu-example:
          ansible_host: 192.168.1.11
          ansible_user: user
          ansible_become: yes
      vars:
        # Common vars
        workstation_user: "{{ ansible_user }}"
